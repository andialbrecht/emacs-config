;; To export the Emacs configuration in batch mode run
;;
;; emacs --batch -l batch.el --visit emacs.org --funcall org-export-as-html

(require 'package)
(package-initialize)
;;(load-theme 'wombat t)
(require 'htmlize)
